/**
 * 给npc赋予一个npc灵魂
 */

#include "npc_controller.h"
#include "config/interface.h"


// 外界调用函数，传入存储npc所能获取的全部信息，返回做出的决定
Betting_Decision makeADecision (int current_index, const NPC* npc_information, PUBLIC_CARDS public_cards) {












}